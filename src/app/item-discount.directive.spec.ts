import { ItemDiscountDirective } from './item-discount.directive';

describe('ItemDiscountDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemDiscountDirective();
    expect(directive).toBeTruthy();
  });
});
